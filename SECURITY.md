# Security Policy

Generated release output is not committed to this repository.

`catalog-acquire` and `catalog-publish` never accept the production private key. Owner-private key material remains outside the repository and ignored by default via `*.private.pem`.
